#!/usr/bin/env bash

r="$HOME/repos"
if [ -e "build.sh" ]; then
    time ./build.sh
    exit
fi

echo "Your current working directory is not on the list! See $(realpath $0)"
echo "Guessing 'time make'..."
time make "$@"
